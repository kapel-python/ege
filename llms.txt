# ege easy — https://egeeasy.ru

> Понятная система подготовки к ЕГЭ: математика (профиль и база) и отдельный
> предмет «Русский язык» с честным заблокированным состоянием первой темы.

## Ключевые страницы

- Лендинг: `/` — описание платформы, предметы, возможности, как работает
- Приложение: `/dashboard` — открывается сразу без регистрации (гостевой профиль);
  вход нужен только для синхронизации между устройствами. Не индексируется.

## Предметы

- Профильная математика: все 20 номеров экзамена (80 заданий, 20 уроков) —
  планиметрия, векторы, стереометрия, вероятности, уравнения, производная,
  текстовые задачи, тригонометрия, неравенства, финансовая математика,
  параметры. Честная граница: новые номера №6 и №13 пока представлены
  одним подтверждённым образцом из демоверсии.
- Базовая математика: все 21 номер экзамена (209 заданий в каталоге, 21 урок);
  доступна практика, диагностика, боссы и прогноз оценки. Часть заданий
  ждёт официальные иллюстрации и отмечается как временно недоступная.
- Русский язык: предмет `russian` с одной темой — «Сочинение по тексту (задание 27)»: 8 заданий с реальными текстами ЕГЭ (GET /api/essay-text). Ученик читает текст в задании и пишет по нему; проверка POST /api/ai/essay по строгой рубрике К1–К6, отчёт — на `/essay/<id>`, XP только после готового отчёта.
- Информатика и обществознание — планируются.

## Как устроена подготовка

1. Диагностика: короткая проверка стартового уровня.
2. Путь по темам: от базовых навыков к сложным заданиям.
3. Тренировка: реальные формулировки ЕГЭ, опыт (XP) и уровни.
4. Работа над ошибками и статистика прогресса.

## API (служебное, не для индексации)

- `/api/bootstrap` — каталог и состояние обучения
- `/api/subjects` — список предметов
- Аутентификация: guest → register → login → logout, сессии в `user_sessions`
